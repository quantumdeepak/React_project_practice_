import React from "react";

let UserContext = React.createContext()

export default UserContext;

//  with the contet we need to make an provider 
//  will add the freagments in usercontext tag i.e. the variable just we created

